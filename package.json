{
  "name": "anglo-tamarineira",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1",
    "verify": "node scripts/verify.mjs",
    "format": "prettier --write src scripts astro.config.mjs package.json tsconfig.json --plugin=prettier-plugin-astro"
  },
  "dependencies": {
    "astro": "^7.3.1"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.0",
    "@playwright/test": "^1.55.0",
    "prettier": "^3.9.6",
    "prettier-plugin-astro": "^0.14.1"
  }
}
